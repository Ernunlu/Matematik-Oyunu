<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.erennl.matematikgelistirme">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

        <activity android:name="com.google.android.gms.ads.AdActivity"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
        android:theme="@android:style/Theme.Translucent" />

        <activity android:name=".Anasayfa"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".oyunsayfa"
            android:theme="@style/AppTheme" />
        <activity android:name=".ayarlar"
            android:theme="@style/AppTheme" />
        <activity android:name=".seceneklericin"
            android:theme="@style/AppTheme" />
        <activity android:name=".toplama"
            android:theme="@style/AppTheme" />
        <activity android:name=".istatislik"
            android:theme="@style/AppTheme" />
        <activity android:name=".cikarma"
            android:theme="@style/AppTheme" />
        <activity android:name=".carpma"
            android:theme="@style/AppTheme" />
        <activity android:name=".bolmea"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>